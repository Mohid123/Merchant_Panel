import { AllowTwoDecimalDirective } from './allow-two-decimal.directive';

describe('AllowTwoDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowTwoDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
