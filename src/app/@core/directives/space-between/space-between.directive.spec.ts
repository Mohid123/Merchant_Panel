import { SpaceBetweenDirective } from './space-between.directive';

describe('SpaceBetweenDirective', () => {
  it('should create an instance', () => {
    const directive = new SpaceBetweenDirective();
    expect(directive).toBeTruthy();
  });
});
